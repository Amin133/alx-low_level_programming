#include <stdio.h>

/**
 * main - print lowwercase alphabet
 *
 * Description: print lowercase alphabet
 *
 * Return: Always 0 (Success)
 */

int main(viod)
{
	int i = 97;

	while (i < 123)
	{
		putchar(i);
		i++;
	}
	puchar(10);

	return (0);
}
